<% content_for :title do %>Forgot your password?<% end %>
<h2>Forgot your password?</h2>

<p>(If you no longer have access to the email account you used when you
  signed up for Festival Fanatic, send me a note using the Feedback link above
  with your name and old & new email addresses, and I'll get you squared
  away quick!)</p>

<%= bootstrap_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= f.alert_message "Please fix the errors below.", error_summary: false %>

  <div class="form-inputs">
    <%= f.email_field :email, :autofocus => true %>
  </div>

  <div class="form-actions well">
    <%= f.submit "Send me password-reset instructions" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
