<%= link_to 'New Location', new_admin_location_path,
            class: 'admin btn btn-sm btn-default' %>
<h1>Listing locations</h1>

<% @locations.group_by(&:place).each do |place, locations| %>
  <div class="place">
    <h2><%= place %></h2>
    <ul class="locations">
      <%= render locations %>
    </ul>
  </div>
<% end %>
