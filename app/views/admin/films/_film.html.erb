<%= content_tag_for(:tr, film) do %>
  <td><%= link_to film.name, [:admin, film] %></td>
  <td><%= flags(film.countries) %></td>
  <td><%= hours_and_minutes(film.duration) %></td>
  <td><%= film.page %></td>
  <td><%= film.url_fragment %></td>
  <td><%= film.screenings.count %></td>
<% end %>
